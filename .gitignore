.next
.swc
tsconfig.tsbuildinfo
certificates
out

node_modules
/.node_modules-pre-daedalos-*

test-results
playwright-report
playwright/.cache

public/private
public/robots.txt
public/rss.xml
public/sitemap.xml
public/.index/*

# Rahul's portfolio is represented by daedalOS's virtual Public desktop and
# documents. These files are authored content, so they must remain versioned.
